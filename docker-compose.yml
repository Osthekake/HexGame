services:
  hexgame:
    build:
      context: .
      dockerfile: docker/combined/Dockerfile
    container_name: hexgame
    restart: unless-stopped
    volumes:
      - ./data:/data
    # App is served at http://localhost/HexGame/
    ports:
      - "80:80"
